# Microsoft Entra Application Configuration
# Copy this file to .env.local and fill in your values

# Microsoft Entra App Registration (OPTIONAL for basic usage)
# If not provided, the app will use Microsoft Graph PowerShell public client ID
# for interactive user authentication
MSAL_CLIENT_ID=your_client_id_here
MSAL_TENANT_ID=your_tenant_id_here
# MSAL_CLIENT_SECRET is not needed for public client authentication

# Microsoft Graph API
GRAPH_API_ENDPOINT=https://graph.microsoft.com/v1.0

# Local LLM Configuration
LLM_PROVIDER=ollama
# LLM_PROVIDER=lmstudio
OLLAMA_BASE_URL=http://localhost:11434
LMSTUDIO_BASE_URL=http://localhost:1234

# MCP Server Configuration
MCP_LOKKA_PORT=3001
MCP_DOCS_PORT=3002
EXTERNAL_MCP_LOKKA_PORT=3003

# External Lokka Configuration
# USE_EXTERNAL_LOKKA=true             # Set to true to use the external Lokka MCP server instead of the built-in one
# LOKKA_TENANT_ID=your_tenant_id_here # Default: falls back to MSAL_TENANT_ID
# LOKKA_CLIENT_ID=your_client_id_here # Default: falls back to MSAL_CLIENT_ID
# LOKKA_CLIENT_SECRET=your_client_secret_here # Required for server-to-server authentication, not needed for interactive auth

# Application Settings
LOG_LEVEL=info
ENABLE_TELEMETRY=false

# Freemium Features
ENABLE_PREMIUM_FEATURES=false
LICENSE_KEY=
