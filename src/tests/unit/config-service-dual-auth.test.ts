// NOTE: This test file is deprecated as Application Credentials mode has been removed
// from EntraPulse Lite in favor of delegated permissions only.
// These tests should be refactored to test Enhanced Graph Access mode instead.

describe('ConfigService - Deprecated Tests', () => {
  test.skip('Application Credentials tests are disabled - mode removed', () => {
    // These tests are skipped because Application Credentials mode
    // has been removed from EntraPulse Lite for security reasons.
    // Only delegated permissions (user token) are supported.
  });
});
